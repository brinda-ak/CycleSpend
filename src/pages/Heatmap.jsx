export default function Heatmap({ profile }) {
  return (
    <div className="p-4">
      <h1 className="text-burgundy font-display font-bold text-xl mb-4">Cycle–Spend Heatmap</h1>
      <p className="text-espresso/80 text-sm">
        Calendar heatmap with phase bands and day detail modal will be built here. Uses transactions from Firestore + cycleUtils + colorUtils.
      </p>
    </div>
  )
}
